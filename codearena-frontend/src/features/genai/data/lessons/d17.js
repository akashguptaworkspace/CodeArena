// Day 17 lessons: The Builder's side: open models, transformers and fine-tuning. Content lives in topic files; this file only picks it (see ../pick.js).
import openModels from "./open-models.js";
import builder from "./builder-track.js";

export default {
  ...openModels,
  ...builder,
};
