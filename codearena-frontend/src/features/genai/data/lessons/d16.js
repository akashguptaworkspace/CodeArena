// Day 16 lessons: LLMOps in production + ship Project 2 + start applying. Content lives in topic files; this file only picks it (see ../pick.js).
import production from "./production.js";
import jobSearch from "./job-search.js";

export default {
  ...production,
  ...jobSearch,
};
