// Node.js catalogue: the light part of the question bank (titles, levels, key points, follow-ups).
// Safe to import anywhere (e.g. the home page). The question text and model answers live in
// content/*.js and are merged in by nodejs.js, which only the Node.js pages import.

const q = (id, title, category, level, core, rubric, twist) => ({
  id: `node-${id}`,
  track: "node",
  title,
  category,
  level,
  core,
  rubric,
  twist,
});

export const NODE_BASE = [
  // ---------- Event loop & runtime ----------
  q("what-is-node", "What is Node.js, and why is it good for I/O-heavy servers?", "Event loop & runtime", "Beginner", true,
    ["V8 runs the JavaScript; libuv provides the event loop and async I/O", "Non-blocking I/O: one thread waits on many sockets instead of one thread per request", "Great for I/O-bound work (APIs, proxies, real-time); weak for CPU-bound work", "One language across frontend and backend, and the npm ecosystem"],
    "Your team wants to build a video-transcoding service in Node. What do you tell them?"),
  q("event-loop-phases", "Explain the Node.js event loop and its phases", "Event loop & runtime", "Intermediate", true,
    ["Phases in order: timers → pending callbacks → poll → check → close callbacks", "Poll phase waits for I/O and runs I/O callbacks", "setImmediate runs in the check phase, timers in the timers phase", "process.nextTick and promise microtasks run between every callback, before the loop moves on"],
    "Where exactly do promise callbacks run relative to these phases?"),
  q("microtasks-order", "Predict the output: nextTick, Promise, setTimeout and setImmediate", "Event loop & runtime", "Intermediate", true,
    ["Synchronous code runs first, top to bottom", "process.nextTick queue drains before the promise microtask queue", "Microtasks all drain before any timer or immediate callback", "setTimeout 0 vs setImmediate order is not guaranteed in the main module"],
    "Wrap the same code inside fs.readFile's callback. Does the order of setTimeout and setImmediate change?"),
  q("settimeout-vs-setimmediate", "setTimeout(fn, 0) vs setImmediate: which runs first?", "Event loop & runtime", "Intermediate", false,
    ["setTimeout 0 is really 1 ms and runs in the timers phase", "setImmediate runs in the check phase, right after poll", "From the main module the order depends on process performance, so it is non-deterministic", "Inside an I/O callback, setImmediate always runs first"],
    "When would you deliberately choose setImmediate over process.nextTick?"),
  q("nexttick-starvation", "What is process.nextTick, and how can it starve the event loop?", "Event loop & runtime", "Intermediate", false,
    ["nextTick callbacks run right after the current operation, before other microtasks and I/O", "The queue is drained completely, including ticks added while draining", "Recursive nextTick never lets the loop reach the poll phase, so I/O starves", "Use it to emit events after a constructor returns or to keep APIs consistently async; prefer setImmediate for yielding"],
    "Does recursive Promise.resolve().then() starve the loop the same way?"),
  q("libuv-threadpool", "What is libuv, and which operations use its thread pool?", "Event loop & runtime", "Advanced", true,
    ["libuv is the C library behind the event loop and cross-platform async I/O", "Network I/O uses OS async primitives (epoll, kqueue, IOCP), not the thread pool", "Thread pool (default 4): fs, dns.lookup, crypto (pbkdf2, scrypt), zlib", "UV_THREADPOOL_SIZE tunes it; a saturated pool makes unrelated fs/crypto calls queue"],
    "Bcrypt logins suddenly make file reads slow on the same server. Why?"),
  q("single-threaded", "If Node is single-threaded, how does it handle 10,000 concurrent requests?", "Event loop & runtime", "Beginner", true,
    ["Your JavaScript runs on one thread; I/O waiting happens in the OS and libuv", "Each request is mostly waiting on DB/network, so one thread can juggle thousands", "Callbacks are queued and run one at a time when their I/O completes", "It breaks down when one request does long CPU work, because everyone waits"],
    "Compare this with a thread-per-request server like classic Java Tomcat. Where does each win?"),
  q("blocking-event-loop", "What blocks the event loop, and how do you detect it in production?", "Event loop & runtime", "Intermediate", true,
    ["Blockers: sync fs/crypto calls, big JSON parse/stringify, heavy loops, catastrophic regexes", "Symptoms: every request slows at once, p99 latency spikes, health checks time out", "Measure event loop lag/delay (perf_hooks.monitorEventLoopDelay) and alert on it", "Fix by chunking work, moving it to worker threads, or streaming"],
    "Your p99 jumps from 50 ms to 3 s every few minutes but CPU looks fine on average. How do you find the cause?"),
  q("v8-basics", "How does V8 run your JavaScript? JIT, hidden classes and deoptimisation", "Event loop & runtime", "Advanced", false,
    ["Ignition interpreter produces bytecode; TurboFan compiles hot functions to optimised machine code", "Hidden classes (shapes) and inline caches make property access fast", "Changing object shapes or mixing types in hot code causes deoptimisation", "Practical rules: initialise all fields in the constructor, keep types stable, avoid delete on hot objects"],
    "Why can adding a property to an object after construction make a hot loop slower?"),
  q("garbage-collection", "How does garbage collection work in V8?", "Event loop & runtime", "Advanced", false,
    ["Generational heap: young generation (new space) and old generation", "Scavenge for young objects: fast, frequent, copies survivors", "Mark-sweep-compact for old space, mostly concurrent/incremental", "Objects reachable from roots survive; long GC pauses show up as latency spikes"],
    "How would you tell from metrics that GC is causing your latency spikes?"),
  q("global-objects", "process, globalThis, __dirname: how is Node different from the browser?", "Event loop & runtime", "Beginner", false,
    ["No window or DOM; globalThis/global is the global object", "process gives env, argv, exit codes, memory usage, signals", "__dirname and __filename exist in CommonJS; in ESM use import.meta.url / import.meta.dirname", "Top-level variables are module-scoped, not global"],
    "Your ESM code throws '__dirname is not defined'. Fix it."),
  q("event-emitter", "How does EventEmitter work? Write a minimal one.", "Event loop & runtime", "Intermediate", true,
    ["Map of event name → array of listeners", "emit calls listeners synchronously, in registration order", "Supports on, once, off; once wraps the listener to remove itself", "An 'error' event with no listener throws; watch for listener leaks (MaxListenersExceededWarning)"],
    "Make emit async so a slow listener can't block the emitter. What changes for callers?"),
  q("timers-accuracy", "Why doesn't setTimeout(fn, 100) fire exactly at 100 ms?", "Event loop & runtime", "Beginner", false,
    ["The delay is a minimum, not a guarantee", "Timers only run when the loop reaches the timers phase", "Long synchronous work or a busy loop delays every timer", "For precise scheduling use a dedicated scheduler or compensate for drift"],
    "Write a setInterval replacement that doesn't drift over an hour."),
  q("process-signals", "How do exit codes and signals (SIGTERM, SIGINT) work in Node?", "Event loop & runtime", "Intermediate", false,
    ["Exit code 0 = success, non-zero = failure; process.exitCode vs process.exit()", "SIGTERM is what Docker/Kubernetes/PM2 send to stop you; SIGINT is Ctrl+C", "Listening for a signal removes the default exit, so you must exit yourself", "SIGKILL can't be caught; you get a grace period before it arrives"],
    "Kubernetes kills your pod after 30 s and in-flight requests fail. What do you change?"),

  // ---------- Async code & errors ----------
  q("callback-hell", "Callbacks, callback hell and error-first callbacks", "Async code & errors", "Beginner", true,
    ["Error-first convention: callback(err, result)", "Callback hell: nested callbacks that are hard to read and handle errors in", "Easy bugs: calling the callback twice, or throwing instead of passing the error", "Fixes: named functions, promises, async/await"],
    "A library calls your callback twice in rare cases. How do you protect against it?"),
  q("promise-states", "How do Promises work? States, chaining, and what .then returns", "Async code & errors", "Beginner", true,
    ["Three states: pending, fulfilled, rejected; settles once", ".then always returns a new promise, which enables chaining", "Returning a promise from .then waits for it; throwing rejects the chain", ".catch handles any earlier rejection; .finally runs either way"],
    "What's the difference between .then(a, b) and .then(a).catch(b)?"),
  q("promise-combinators", "Promise.all vs allSettled vs race vs any", "Async code & errors", "Intermediate", true,
    ["all: resolves with every value, rejects on the first rejection", "allSettled: waits for all, returns status for each", "race: settles with the first to settle, success or failure", "any: first success; rejects with AggregateError only if all fail"],
    "Promise.all rejects early, but do the other requests stop running?"),
  q("async-await-internals", "How does async/await work under the hood?", "Async code & errors", "Intermediate", false,
    ["An async function always returns a promise", "await pauses the function and schedules the rest as a microtask when the promise settles", "It is syntax over promises and generators; the thread is never blocked", "try/catch works with await; errors become rejections"],
    "What does this log: an async function that logs 'A', awaits nothing, then logs 'B', called before a sync log 'C'?"),
  q("sequential-vs-parallel", "Predict the timing: awaiting in a loop vs Promise.all", "Async code & errors", "Intermediate", true,
    ["await inside a for loop runs requests one after another: total ≈ sum of times", "Start all promises first, then Promise.all: total ≈ the slowest one", "forEach with an async callback does not wait at all", "Use sequential only when order or rate limits require it"],
    "You have 10,000 items. Why is Promise.all over all of them a bad idea?"),
  q("concurrency-limit", "Run 1,000 API calls with at most 5 in flight", "Async code & errors", "Advanced", true,
    ["Worker-pool pattern: N workers pull from a shared queue/index", "Keep results in input order", "Decide on failure behaviour: fail fast or collect errors", "Libraries (p-limit, p-map) do this; know how to write it"],
    "The third-party API also allows only 20 requests per second. Add that limit."),
  q("unhandled-rejection", "Unhandled rejections and uncaught exceptions: what happens and what to do", "Async code & errors", "Intermediate", true,
    ["Since Node 15 an unhandled rejection crashes the process by default", "uncaughtException means state may be corrupt: log, then exit", "Use process handlers for logging and graceful exit, not to keep running", "Let the process manager restart it; fix the missing catch"],
    "Someone added process.on('uncaughtException', () => {}) to 'stop crashes'. What's wrong?"),
  q("promisify", "Convert a callback API to promises (util.promisify, and write your own)", "Async code & errors", "Beginner", false,
    ["util.promisify works with error-first callbacks", "Your own version: return new Promise and call resolve/reject from the callback", "Preserve this binding when promisifying methods", "Many core modules already have promise versions (fs/promises, timers/promises)"],
    "The callback gets two result values (err, a, b). How do you promisify it?"),
  q("async-error-express", "Why do async errors crash or hang an Express 4 app, and how do you fix it?", "Async code & errors", "Intermediate", true,
    ["Express 4 only catches errors thrown synchronously in handlers", "A rejected promise in an async handler is unhandled: request hangs or process crashes", "Fix: wrap handlers (asyncHandler), call next(err), or use Express 5 which handles promises", "Have one error middleware that turns errors into responses"],
    "Write the asyncHandler wrapper in one line."),
  q("retry-backoff", "Write a retry with exponential backoff and jitter", "Async code & errors", "Intermediate", false,
    ["Retry only transient errors (timeouts, 5xx, 429), never 4xx validation errors", "Delay doubles each attempt with a cap", "Add random jitter so clients don't retry in sync", "Only retry idempotent operations, or use an idempotency key"],
    "Your retries made an outage worse. What's a retry budget or circuit breaker?"),
  q("timeout-abort", "Add a timeout to a promise and cancel work with AbortController", "Async code & errors", "Intermediate", false,
    ["Promise.race with a timer rejects late but does not stop the work", "AbortController/AbortSignal actually cancels fetch, streams, timers", "AbortSignal.timeout(ms) and AbortSignal.any for combining", "Always clear the timer to avoid keeping the process alive"],
    "The client disconnects mid-request. How do you cancel the downstream DB/HTTP work?"),
  q("async-local-storage", "What is AsyncLocalStorage, and when would you use it?", "Async code & errors", "Advanced", false,
    ["Stores context that follows an async call chain (like thread-local storage)", "Typical use: request ID, user ID, transaction for logging and tracing", "run(store, fn) sets it; getStore() reads it anywhere below", "Some cost per async hop; context can be lost with custom thenables or queues"],
    "Every log line should include the request ID without passing it through 20 functions. Show how."),

  // ---------- Modules & npm ----------
  q("cjs-vs-esm", "CommonJS vs ES modules", "Modules & npm", "Beginner", true,
    ["CJS: require/module.exports, synchronous, resolved at runtime", "ESM: import/export, static, async loading, top-level await", "Selected by .mjs/.cjs or \"type\" in package.json", "Interop: ESM can import CJS; CJS needs dynamic import() for ESM (or require(esm) in newer Node)"],
    "Why can bundlers tree-shake ESM but not CommonJS?"),
  q("require-cache", "How does require() resolve and cache modules?", "Modules & npm", "Intermediate", false,
    ["Resolution: core modules, relative paths, then node_modules walking up the tree", "The module code runs once; later requires get the cached exports", "Modules are wrapped in a function giving exports, require, module, __filename, __dirname", "Cache is keyed by resolved path; delete require.cache[path] to reload"],
    "Two copies of the same library end up in node_modules. What breaks?"),
  q("circular-deps", "What happens with circular dependencies?", "Modules & npm", "Advanced", false,
    ["CJS: the second module gets a partially filled exports object", "ESM: live bindings, but accessing before initialisation throws a TDZ error", "Symptom: undefined is not a function at startup", "Fix: move shared code to a third module, or require lazily inside functions"],
    "How would you find circular imports in a large codebase?"),
  q("module-exports", "module.exports vs exports", "Modules & npm", "Beginner", false,
    ["exports is a shorthand reference to module.exports", "require returns module.exports", "Assigning exports = {...} breaks the link and exports nothing", "Use exports.x = ... for properties, module.exports = ... to replace"],
    "What does require return if a module sets both exports.a and module.exports = { b }?"),
  q("package-lock", "package.json vs package-lock.json, and semver ranges", "Modules & npm", "Beginner", true,
    ["package.json lists allowed ranges; the lock file pins exact resolved versions", "Semver: MAJOR.MINOR.PATCH; ^ allows minor+patch, ~ allows patch", "Commit the lock file so every machine and CI installs the same tree", "Lock files also record integrity hashes"],
    "A patch release of a dependency broke production even though you didn't change code. How?"),
  q("deps-types", "dependencies vs devDependencies vs peerDependencies", "Modules & npm", "Beginner", false,
    ["dependencies: needed at runtime", "devDependencies: build, test and lint tools only", "peerDependencies: the host app must provide it (plugins, React components)", "Production installs can skip dev dependencies (npm ci --omit=dev)"],
    "Your Docker image is 1.2 GB. Where do you start?"),
  q("npm-ci", "npm install vs npm ci", "Modules & npm", "Beginner", false,
    ["npm ci installs exactly what the lock file says", "It fails if package.json and the lock file disagree", "It deletes node_modules first: clean and reproducible", "Use npm ci in CI and Docker builds; npm install when changing dependencies"],
    "CI passes but production crashes with a missing module. What would you check?"),
  q("supply-chain", "How do you keep npm dependencies secure?", "Modules & npm", "Intermediate", false,
    ["npm audit plus automated updates (Dependabot/Renovate)", "Lock files and npm ci so nothing changes silently", "Review install scripts; consider --ignore-scripts and minimal dependencies", "Watch for typosquatting and compromised maintainers; pin and verify"],
    "A popular package you use just published a malicious patch version. What limits the damage?"),

  // ---------- Streams, buffers & files ----------
  q("buffer", "What is a Buffer, and why does Node need it?", "Streams, buffers & files", "Beginner", true,
    ["A fixed-size chunk of raw binary memory outside the V8 heap", "Needed for files, sockets, images, crypto: data that isn't text", "Convert with encodings: toString('utf8'), Buffer.from(str, 'base64')", "Buffer.alloc is zero-filled; allocUnsafe is faster but may contain old data"],
    "Why is Buffer.allocUnsafe a potential security issue?"),
  q("streams-types", "Stream types: readable, writable, duplex and transform", "Streams, buffers & files", "Intermediate", true,
    ["Readable: source (file read, HTTP request)", "Writable: destination (file write, HTTP response)", "Duplex: both, independent (TCP socket); Transform: output derived from input (gzip)", "Streams process data in chunks, so memory stays flat regardless of size"],
    "Is an HTTP request object on the server a readable or writable stream? And the response?"),
  q("backpressure", "What is backpressure, and how do pipe and pipeline handle it?", "Streams, buffers & files", "Advanced", true,
    ["Backpressure: the consumer is slower than the producer", "write() returns false when the internal buffer passes highWaterMark", "Stop writing and wait for 'drain' before continuing", "pipe/pipeline pause and resume the source automatically"],
    "You ignore write()'s return value while copying a huge file to a slow network. What happens?"),
  q("large-file", "Read a 10 GB file without running out of memory", "Streams, buffers & files", "Intermediate", true,
    ["readFile loads the whole file into memory and fails or crashes", "Use fs.createReadStream and process chunk by chunk", "Use pipeline to connect transforms and a writable destination", "For lines, use readline or a line-splitting transform"],
    "Now count how many times each IP appears. What if there are 50 million unique IPs?"),
  q("pipeline-errors", "pipe() vs stream.pipeline(): error handling and cleanup", "Streams, buffers & files", "Intermediate", false,
    ["pipe doesn't forward errors; each stream needs its own handler", "On error, pipe leaves streams open: leaked file descriptors", "pipeline forwards errors and destroys all streams on failure", "Use the promise version: await pipeline(a, b, c)"],
    "The client disconnects halfway through a download. What does pipeline do with the file stream?"),
  q("transform-stream", "Write a Transform stream (e.g. uppercase lines or CSV → JSON)", "Streams, buffers & files", "Advanced", false,
    ["Implement transform(chunk, encoding, callback) and push output", "Chunks don't respect line boundaries: keep a leftover buffer", "Handle the last partial line in flush()", "Use objectMode when emitting objects instead of bytes"],
    "How would you make the transform process rows in batches of 500 for bulk DB inserts?"),
  q("fs-sync-async", "fs sync vs callback vs promises API: when to use which", "Streams, buffers & files", "Beginner", false,
    ["Sync calls block the event loop: fine at startup or in CLI scripts", "Never use sync calls in a request handler", "fs/promises with async/await for normal async code", "Streams for large files"],
    "Is readFileSync at the top of a module (loading a config) a problem?"),
  q("file-upload", "Handle a large file upload without buffering it in memory", "Streams, buffers & files", "Intermediate", true,
    ["Stream the multipart body (busboy/multer disk storage) instead of buffering", "Enforce size and type limits while streaming, not after", "Better at scale: pre-signed URL so the client uploads directly to S3", "Scan/process asynchronously after upload via a queue"],
    "Users on flaky mobile networks keep failing 2 GB uploads at 90%. What do you do?"),
  q("stream-http-response", "Stream a large DB export as a CSV download", "Streams, buffers & files", "Intermediate", false,
    ["Don't load all rows into memory: use a DB cursor/stream", "Transform rows to CSV lines and pipe to the response", "Set Content-Type and Content-Disposition headers", "Respect backpressure and stop the query if the client disconnects"],
    "The export takes 10 minutes and times out at the load balancer. Redesign it."),
  q("encoding", "Encodings: utf8, base64, and multibyte characters split across chunks", "Streams, buffers & files", "Advanced", false,
    ["UTF-8 characters can be 1–4 bytes", "Calling chunk.toString() per chunk can break a character across chunks", "Use setEncoding('utf8') or StringDecoder to handle partial characters", "base64 is for binary-in-text; it grows data by about 33%"],
    "Hindi text in your uploaded CSV shows garbage characters at random points. Why?"),
  q("readline", "Process a huge log file line by line", "Streams, buffers & files", "Beginner", false,
    ["readline.createInterface over a read stream", "for await (const line of rl) keeps memory flat", "crlfDelay: Infinity handles Windows line endings", "Aggregate as you go; don't keep every line"],
    "Find the 10 slowest requests in a 5 GB access log. What data structure do you keep?"),

  // ---------- Performance & scaling ----------
  q("cluster", "The cluster module: how it works and when to use it", "Performance & scaling", "Intermediate", true,
    ["One Node process uses one CPU core for JavaScript", "Cluster forks one worker per core; the primary distributes connections", "Workers share nothing: in-memory state and sessions must move out (Redis)", "In containers, prefer one process per container and scale replicas instead"],
    "You use cluster with an in-memory rate limiter. What bug do you have?"),
  q("worker-threads", "worker_threads vs cluster vs child_process", "Performance & scaling", "Intermediate", true,
    ["worker_threads: threads in one process for CPU work, can share memory (SharedArrayBuffer)", "cluster: multiple processes serving the same port", "child_process: run other programs or scripts (spawn, exec, fork)", "Use a worker pool (e.g. piscina) instead of a new worker per task"],
    "Generating a PDF takes 2 s of CPU per request. Which one and why?"),
  q("cpu-heavy", "A CPU-heavy endpoint is slowing every request. Fix it.", "Performance & scaling", "Intermediate", true,
    ["Confirm with event loop lag and profiling that it's CPU on the main thread", "Move the work to a worker pool so the loop stays free", "Or make it async: queue a job and return 202 with a job ID", "Cache results and consider a separate service in a better-suited language"],
    "The work takes 30 s. What does the API contract look like?"),
  q("memory-leak", "Find and fix a memory leak in production", "Performance & scaling", "Advanced", true,
    ["Confirm: heap used grows steadily across GCs and never comes back down", "Take heap snapshots (--inspect, heapdump, --heapsnapshot-signal) some time apart", "Compare snapshots to find which objects grow and what retains them", "Fix the retainer, then verify under load"],
    "The container restarts from OOM every 6 hours but you can't attach a debugger. What do you do?"),
  q("common-leaks", "Common causes of memory leaks in Node", "Performance & scaling", "Intermediate", true,
    ["Unbounded caches or maps keyed by user/request", "Event listeners added per request and never removed", "Closures and timers (setInterval) holding large objects", "Global arrays used as logs/queues that only grow"],
    "Build a cache that can't leak. What limits do you add?"),
  q("heap-limit", "Heap limits, --max-old-space-size, and OOM crashes in containers", "Performance & scaling", "Advanced", false,
    ["V8 has its own heap limit, separate from the container limit", "Newer Node versions size the heap from the cgroup memory limit", "Set --max-old-space-size below the container limit to leave room for buffers and native memory", "RSS includes Buffers and native memory outside the heap"],
    "Heap is 300 MB but RSS is 1.5 GB. Where is the memory?"),
  q("profiling", "How do you profile a slow Node service?", "Performance & scaling", "Advanced", false,
    ["Measure first: which endpoint, p50/p99, CPU vs waiting on I/O", "CPU profile with --inspect/Chrome DevTools or --cpu-prof, read flame graphs", "Tools: clinic.js (doctor, flame, bubbleprof), 0x", "For I/O waits: tracing spans show slow DB/HTTP calls"],
    "The flame graph shows most time in JSON.stringify. What are your options?"),
  q("pm2", "PM2 and process managers: what do they give you?", "Performance & scaling", "Beginner", false,
    ["Restart on crash, and on reboot", "Cluster mode across cores", "Log management and monitoring", "Zero-downtime reload; in Kubernetes the orchestrator does most of this instead"],
    "Your app crashes on startup in a loop under PM2. What protects the server?"),
  q("horizontal-scaling", "Scale a Node API horizontally: what must change?", "Performance & scaling", "Intermediate", true,
    ["Stateless instances behind a load balancer", "Move sessions, caches, rate limits and locks to shared stores (Redis)", "File uploads to object storage, not local disk", "Cron jobs and WebSockets need coordination (locks, pub/sub, sticky sessions)"],
    "WebSocket users on server A must receive messages sent by users on server B."),
  q("caching-in-memory", "In-process cache vs Redis: trade-offs", "Performance & scaling", "Intermediate", false,
    ["In-process: fastest, but per-instance, lost on restart, costs heap", "Redis: shared across instances, survives restarts, one network hop", "Invalidation is harder with many in-process copies", "Common: small in-process LRU for hot config + Redis for shared data"],
    "Config changes must reach all 20 instances within 5 seconds. How?"),
  q("connection-pooling", "Why use a DB connection pool, and how do you size it?", "Performance & scaling", "Intermediate", true,
    ["Opening a connection per query is slow (TCP + TLS + auth)", "The pool reuses a fixed number of connections and queues extra queries", "Total connections = pool size × instances; must stay under the DB max", "Always release connections, including on errors; set acquire timeouts"],
    "You scale from 4 to 40 instances and the database starts refusing connections."),
  q("keepalive", "HTTP keep-alive and agent settings for outbound calls", "Performance & scaling", "Advanced", false,
    ["Without keep-alive every outbound call pays a new TCP/TLS handshake", "Use an agent with keepAlive (the default in newer Node; undici for fetch)", "Limit max sockets per host to protect downstream services", "Mismatched idle timeouts cause ECONNRESET on reused sockets"],
    "You get random ECONNRESET errors calling another service behind a load balancer. Why?"),
  q("json-large", "JSON.parse/stringify on big payloads blocks the loop. Options?", "Performance & scaling", "Advanced", false,
    ["JSON parse/stringify is synchronous and CPU-bound", "Avoid huge payloads: paginate, select fewer fields", "Stream JSON (NDJSON, streaming parsers) instead of one huge document", "Offload to worker threads or use schema-based fast serializers (fast-json-stringify)"],
    "Your API returns a 40 MB JSON response. What do you change first?"),

  // ---------- HTTP, Express & APIs ----------
  q("http-server", "Build an HTTP server without Express", "HTTP, Express & APIs", "Beginner", false,
    ["http.createServer((req, res) => ...)", "Route by req.method and URL", "The body arrives as a stream: collect chunks, then parse", "Set status and headers, then res.end; handle errors and body size limits"],
    "What does Express actually add on top of this?"),
  q("express-middleware", "How does Express middleware work? Order and next()", "HTTP, Express & APIs", "Beginner", true,
    ["Middleware is (req, res, next); runs in the order it's registered", "Either end the response or call next()", "next(err) skips to error middleware", "Examples: body parsing, auth, logging, CORS, rate limiting"],
    "Your auth middleware runs, but some routes are still public. Why might that happen?"),
  q("error-middleware", "Centralised error handling in Express", "HTTP, Express & APIs", "Intermediate", true,
    ["Error middleware has four args: (err, req, res, next), registered last", "Custom error classes with status codes (operational vs programmer errors)", "Hide internal details and stack traces in production", "Log with request context; return a consistent error shape"],
    "How do you make a DB unique-constraint error come back as a clean 409?"),
  q("rest-design", "Design a REST API: resources, methods, status codes, versioning", "HTTP, Express & APIs", "Intermediate", true,
    ["Nouns for resources, HTTP methods for actions", "Correct status codes: 200, 201, 204, 400, 401, 403, 404, 409, 422, 429, 500", "Idempotency: GET, PUT, DELETE are; POST isn't", "Versioning (/v1 or headers), consistent errors, pagination and filtering"],
    "How do you model an action like 'cancel order' in REST?"),
  q("pagination", "Offset vs cursor pagination", "HTTP, Express & APIs", "Intermediate", true,
    ["Offset/limit is simple but slow on deep pages (DB scans skipped rows)", "Offset pages shift when rows are inserted: duplicates and gaps", "Cursor: WHERE (created_at, id) < (last values) with an index; stable and fast", "Cursors can't jump to page 50; return an opaque next cursor"],
    "Sort by 'most liked' where like counts change constantly. How does the cursor work?"),
  q("idempotency", "Make a payment endpoint idempotent", "HTTP, Express & APIs", "Advanced", true,
    ["Client sends an Idempotency-Key header per logical operation", "Store key → status/response with a unique constraint", "Replay returns the stored response instead of charging again", "Handle concurrent duplicates (in-progress state/lock) and expire keys"],
    "Two identical requests arrive 5 ms apart on different servers. Walk through it."),
  q("rate-limit-impl", "Implement rate limiting in an Express API", "HTTP, Express & APIs", "Intermediate", true,
    ["Choose an algorithm: fixed window, sliding window, token bucket", "Key by user ID or API key, IP as fallback (careful behind proxies: trust proxy)", "Store counters in Redis with atomic INCR + EXPIRE across instances", "Return 429 with Retry-After and rate limit headers"],
    "Behind a load balancer every request seems to come from the same IP. Fix it."),
  q("validation", "Where and how do you validate request input?", "HTTP, Express & APIs", "Beginner", false,
    ["Validate at the edge: body, params, query, headers", "Schema libraries: zod, joi, ajv (JSON Schema)", "Reject unknown fields; coerce types carefully", "Return 400/422 with field-level errors; still enforce constraints in the DB"],
    "Why isn't frontend validation enough?"),
  q("cors", "What is CORS, and how do you configure it?", "HTTP, Express & APIs", "Beginner", true,
    ["The browser blocks cross-origin reads unless the server allows them", "Access-Control-Allow-Origin and friends; preflight OPTIONS for non-simple requests", "With cookies: credentials: true and an explicit origin, never *", "CORS is a browser rule, not server security: curl ignores it"],
    "Your API works in Postman but fails in the browser. Walk through debugging it."),
  q("file-structure", "How do you structure a large Node/Express codebase?", "HTTP, Express & APIs", "Intermediate", true,
    ["Layers: routes → controllers → services → data access", "Keep business logic out of route handlers so it's testable", "Group by feature/module as the app grows", "Central config, error handling, logging; dependency injection for testability"],
    "Where does a rule like 'a user can have at most 3 active orders' live, and why?"),
  q("graceful-shutdown", "Graceful shutdown of an HTTP server", "HTTP, Express & APIs", "Intermediate", true,
    ["Listen for SIGTERM/SIGINT", "Stop accepting new connections (server.close) and fail readiness checks", "Let in-flight requests finish, with a hard timeout", "Close DB pools, queues and other resources, then exit"],
    "Idle keep-alive connections keep server.close() from ever finishing. Fix it."),
  q("websockets", "WebSockets vs SSE vs long polling in Node", "HTTP, Express & APIs", "Intermediate", false,
    ["WebSocket: full-duplex, good for chat and games", "SSE: server → client only, over HTTP, auto-reconnect; good for notifications and feeds", "Long polling: fallback, higher overhead", "Scaling: sticky sessions or pub/sub (Redis) to fan out across instances"],
    "A live cricket score page for 1M viewers. Which one and why?"),
  q("express-vs-fastify", "Express vs Fastify vs NestJS", "HTTP, Express & APIs", "Beginner", false,
    ["Express: minimal, huge ecosystem, older design", "Fastify: faster, schema-based validation and serialisation, plugin system", "NestJS: opinionated structure, DI, decorators; on top of Express or Fastify", "Choice depends on team size, structure needs and performance"],
    "When would you not reach for NestJS?"),
  q("http-caching", "Compression, ETags and caching headers for APIs", "HTTP, Express & APIs", "Intermediate", false,
    ["Compress responses (gzip/brotli), ideally at the proxy/CDN", "Cache-Control: max-age, no-store, private vs public", "ETag/If-None-Match to return 304 Not Modified", "Don't cache personalised responses on shared caches"],
    "A CDN served user A's profile to user B. What headers were wrong?"),

  // ---------- Databases, caching & queues ----------
  q("sql-injection", "SQL injection and parameterised queries in Node", "Databases, caching & queues", "Beginner", true,
    ["Never build SQL with string concatenation from user input", "Use placeholders (? or $1) so values are sent separately", "ORMs help but raw queries inside them can still be vulnerable", "Whitelist dynamic identifiers like sort columns"],
    "The client sends a sort column name. How do you use it safely?"),
  q("orm-vs-raw", "ORM vs query builder vs raw SQL", "Databases, caching & queues", "Intermediate", false,
    ["ORM (Sequelize, Prisma, TypeORM): productivity, models, migrations", "Query builder (Knex): SQL control with composability", "Raw SQL: full control and performance, more boilerplate", "ORMs can hide N+1 and inefficient queries: log and inspect SQL"],
    "The ORM query for a report takes 8 s. What do you do?"),
  q("transactions", "Run a database transaction in Node (money transfer)", "Databases, caching & queues", "Intermediate", true,
    ["All steps use the same connection/transaction object", "Commit on success, roll back on any error (try/catch/finally)", "Lock or conditionally update to prevent races (SELECT ... FOR UPDATE, WHERE balance >= amount)", "Keep transactions short: no external API calls inside"],
    "Two transfers from the same account run at the same moment. Show how the balance can go negative, and prevent it."),
  q("n-plus-one", "The N+1 query problem", "Databases, caching & queues", "Intermediate", true,
    ["1 query for the list + 1 query per item = N+1 round trips", "Usually hidden by ORM lazy loading or loops with await", "Fix with a JOIN, IN (...) batch query, or eager loading", "In GraphQL, use DataLoader to batch per request"],
    "How would you detect N+1 queries automatically in CI?"),
  q("redis-cache", "Cache-aside with Redis: invalidation and stampede", "Databases, caching & queues", "Intermediate", true,
    ["Cache-aside: read cache, on miss read DB and populate with TTL", "Invalidate or update on write; TTL as a safety net", "Stampede: many misses at once hit the DB; use locks/single-flight or early refresh", "Handle Redis being down: fall back to DB with limits"],
    "A product price is updated but users see the old price for 10 minutes. Fix the flow."),
  q("job-queue", "Move slow work to a background queue (BullMQ)", "Databases, caching & queues", "Intermediate", true,
    ["API enqueues a job and responds quickly; workers process separately", "Retries with backoff and a dead-letter/failed state", "Jobs must be idempotent: at-least-once delivery means duplicates", "Concurrency limits, priorities, scheduled/repeat jobs, monitoring"],
    "A worker crashes mid-job. What does the queue do, and what must your job code handle?"),
  q("distributed-lock", "Run a cron job only once across 5 instances", "Databases, caching & queues", "Advanced", false,
    ["Running cron in every instance runs the job 5 times", "Options: a single scheduler instance, a queue's repeatable jobs, or a lock", "Redis lock: SET key value NX PX ttl, release only if you own it", "Locks can expire mid-job: make the job idempotent too"],
    "The job sometimes takes longer than the lock TTL. What goes wrong?"),
  q("mongo-vs-sql", "MongoDB vs MySQL/Postgres for a Node service", "Databases, caching & queues", "Beginner", false,
    ["SQL: relations, joins, strong constraints and transactions", "MongoDB: flexible documents, nested data, easy horizontal scaling", "Choose from access patterns and consistency needs, not hype", "Mongo also has transactions now, but modelling differs (embed vs reference)"],
    "Which would you pick for an e-commerce order system, and why?"),
  q("outbox", "Send an email only if the DB transaction commits (outbox pattern)", "Databases, caching & queues", "Advanced", false,
    ["Problem: DB commit and email/queue publish can't be atomic", "Write the event to an outbox table in the same transaction", "A relay process reads the outbox and publishes, then marks sent", "At-least-once: consumers must dedupe"],
    "Why not just send the email after commit() returns?"),
  q("migrations", "Schema migrations with zero downtime", "Databases, caching & queues", "Advanced", false,
    ["Version-controlled migrations run in CI/CD", "Expand → migrate → contract: add new, dual-write/backfill, remove old later", "Old and new app versions run at the same time during deploys", "Watch for table locks on big tables (online DDL tools)"],
    "Rename a column on a 50M-row table with zero downtime."),

  // ---------- Security & auth ----------
  q("jwt", "How does JWT authentication work, and what are its weaknesses?", "Security & auth", "Intermediate", true,
    ["Header.payload.signature; signed, not encrypted", "Server verifies the signature and expiry without a DB lookup", "Hard to revoke before expiry: keep access tokens short-lived", "Pin the algorithm, use strong secrets/keys, don't put secrets in the payload"],
    "A user's account is compromised. How do you log them out everywhere?"),
  q("refresh-tokens", "Access and refresh tokens: storage and rotation", "Security & auth", "Advanced", true,
    ["Short-lived access token (minutes), long-lived refresh token (days)", "Refresh token in an httpOnly, Secure, SameSite cookie; access token in memory", "Rotate refresh tokens on every use and store only their hash", "Reuse of an old refresh token means theft: revoke the whole family"],
    "Two browser tabs refresh at the same moment. One gets 'token reused'. How do you handle it?"),
  q("session-vs-jwt", "Sessions vs JWT", "Security & auth", "Intermediate", true,
    ["Session: random ID in a cookie, state in a store; easy to revoke", "JWT: self-contained, stateless verification, harder to revoke", "Sessions need a shared store (Redis) across instances", "For a normal web app, sessions are often simpler and safer"],
    "When is JWT clearly the better choice?"),
  q("password-hashing", "Store passwords safely (bcrypt, argon2)", "Security & auth", "Beginner", true,
    ["Never store plain text or fast hashes (MD5, SHA-256)", "Use slow, salted hashes: bcrypt, scrypt, argon2id", "Tune the cost factor; use the async API so it doesn't block the loop", "Rate-limit login and use constant-time comparison"],
    "Your login endpoint gets 10,000 requests/sec during an attack. Bcrypt is eating all CPU. What now?"),
  q("owasp", "Top security risks for a Node API, and how you fix them", "Security & auth", "Intermediate", true,
    ["Broken access control: check ownership on every resource (IDOR)", "Injection: parameterise queries, validate input", "Security headers (helmet), HTTPS, CORS done right", "Rate limiting, dependency updates, no secrets or stack traces in responses"],
    "GET /api/orders/1234 returns another user's order. What's the fix, and where does it live?"),
  q("xss-csrf", "XSS and CSRF: what they are and how to prevent them", "Security & auth", "Intermediate", false,
    ["XSS: attacker script runs in your page; escape output, Content-Security-Policy", "httpOnly cookies stop scripts reading the session", "CSRF: another site triggers requests with your cookies", "SameSite cookies, CSRF tokens, and checking Origin prevent CSRF"],
    "You store the JWT in localStorage. Which attack does that expose you to?"),
  q("secrets", "Manage secrets and config (env vars, 12-factor)", "Security & auth", "Beginner", false,
    ["Config in environment variables, not in code", "Never commit .env; provide .env.example", "Use a secret manager (AWS Secrets Manager, Vault) in production", "Validate config at startup and fail fast"],
    "A secret was pushed to a public GitHub repo 10 minutes ago. What do you do, in order?"),
  q("prototype-pollution", "What is prototype pollution?", "Security & auth", "Advanced", false,
    ["Attacker sets __proto__ or constructor.prototype via merged JSON input", "Every object then inherits the attacker's property", "Can bypass checks like if (user.isAdmin) or cause crashes", "Fix: validate schemas, block these keys, use Object.create(null) or Map"],
    "Show how a naive deep-merge of req.body can make every user an admin."),
  q("redos", "ReDoS and other ways one request can take down a Node server", "Security & auth", "Advanced", false,
    ["Catastrophic backtracking regex takes seconds of CPU on crafted input", "Because Node is single-threaded, that blocks everyone", "Also: huge JSON bodies, deep nesting, zip bombs", "Fix: safe regexes/RE2, body size limits, timeouts, input length limits"],
    "Which regex in an email validator could be vulnerable, and how do you test it?"),
  q("oauth", "The OAuth 2.0 / Google sign-in flow", "Security & auth", "Intermediate", false,
    ["Authorization code flow with PKCE for web and mobile", "Redirect → consent → code → exchange for tokens on the server", "Verify the ID token (signature, audience, issuer, expiry)", "Then create your own session; don't use Google's token as your session"],
    "What's the state parameter for, and what attack does it stop?"),

  // ---------- Testing & production ----------
  q("testing-pyramid", "Unit vs integration vs end-to-end tests for a Node API", "Testing & production", "Beginner", true,
    ["Unit: pure logic, fast, many", "Integration: routes with a real DB (Testcontainers/test DB)", "E2E: full flows, few, slowest", "Tools: node:test / Jest / Vitest, supertest for HTTP"],
    "Your tests pass but production breaks on a DB migration. What kind of test was missing?"),
  q("mocking", "Mock database and HTTP calls in tests", "Testing & production", "Intermediate", false,
    ["Dependency injection makes mocking simple", "Mock external HTTP with nock/msw, not real calls", "Prefer a real DB for data-layer tests; mock at the boundary", "Over-mocking tests the mocks, not the code"],
    "Test a function that calls Date.now() and setTimeout without waiting in real time."),
  q("logging", "Structured logging and request IDs", "Testing & production", "Intermediate", true,
    ["JSON logs (pino) that a log system can search", "Log levels, and no secrets or PII in logs", "Request/correlation ID on every log line, passed to downstream services", "console.log is synchronous to terminals/files and can slow a hot path"],
    "A user reports 'payment failed at 3:42 pm'. How do you find everything that happened?"),
  q("health-checks", "Health checks: liveness vs readiness", "Testing & production", "Intermediate", false,
    ["Liveness: is the process alive and not stuck? Failing it restarts the container", "Readiness: can it serve traffic now (DB connected, warmed up)?", "Don't put downstream dependencies in liveness: cascading restarts", "Fail readiness during shutdown so the load balancer drains you"],
    "The DB goes down and Kubernetes restarts all 50 pods in a loop. What was misconfigured?"),
  q("docker", "Dockerise a Node app properly", "Testing & production", "Intermediate", true,
    ["Small base image, multi-stage build, npm ci --omit=dev", "Copy package files first to cache the dependency layer", "Run as a non-root user; .dockerignore node_modules and secrets", "Run node directly (not npm start) so signals reach the process"],
    "Your container ignores docker stop and is killed after 10 s. Why?"),
  q("debugging", "Debug a Node process (inspector, logs, diagnostics)", "Testing & production", "Beginner", false,
    ["node --inspect and Chrome DevTools or VS Code breakpoints", "Read stack traces; enable async stack traces", "Diagnostic reports (--report-on-fatalerror) and heap snapshots", "Reproduce locally with the same data and Node version"],
    "A bug only happens in production and you can't attach a debugger. What do you do?"),
  q("observability", "Metrics and tracing for a Node service", "Testing & production", "Advanced", false,
    ["RED metrics per route: rate, errors, duration (p50/p95/p99)", "Runtime metrics: event loop lag, heap, GC, active handles", "Distributed tracing with OpenTelemetry across services", "Alert on symptoms users feel, not every metric"],
    "Latency went up after a deploy. Which metrics/traces do you look at first?"),
  q("zero-downtime-deploy", "Deploy a Node API with zero downtime", "Testing & production", "Advanced", false,
    ["Rolling or blue-green deploys behind a load balancer", "Readiness checks gate traffic; graceful shutdown drains old instances", "Backward-compatible DB migrations and API changes", "Fast rollback and health-based automatic rollback"],
    "The new version has a bug that corrupts data. How does your process limit the damage?"),
];

export const NODE_QUESTION_COUNT = NODE_BASE.length;
