# Local development defaults (committed; no secrets). Put your own values in .env.local.
# The backend (codearena-backend) serves live presence on the same port as the API.
VITE_PRESENCE_URL=ws://localhost:4000/presence
